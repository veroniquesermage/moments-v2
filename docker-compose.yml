services:
  postgres:
    image: postgres:16
    container_name: moments-postgres
    environment:
      POSTGRES_DB: moments
      POSTGRES_USER: moments
      POSTGRES_PASSWORD: moments
    ports:
      - "5432:5432"
    volumes:
      - moments_postgres_data:/var/lib/postgresql/data

volumes:
  moments_postgres_data: {}
